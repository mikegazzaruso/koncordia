{
  "name": "@koncordia/driver",
  "version": "0.1.0",
  "description": "Local driver: alternates Claude Code and Codex CLI turns in a Koncordia room until consensus.",
  "license": "MIT",
  "type": "module",
  "bin": { "koncordia": "dist/cli.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@koncordia/server": "*"
  },
  "engines": { "node": ">=20" }
}
